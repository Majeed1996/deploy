const obj = {
    name : "Abdul Majeed",
    gender : "Male",
    address : {
        city : "Thanjavur",
        country : "India"
    }
};

const array=["muskmelon"," apple", "orange", "fig"];

function add(a,b) {
    return a+b;
}

export default obj;
export {array,add};